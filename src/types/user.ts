export type TUsers = {
  type?: number;
  page?: number;
  limit?: number;
  search?: string;
  startDate?: string;
  endDate?: string;
};

export type TUserDetails = {
  userId: string;
  // id?: number;
  // fullName?: string;
  // username?: string;
  // email?: string;
  // contactNumber?: string;
  // role?: number;
  // isActive?: boolean;
  // createdAt?: string;
  // updatedAt?: string;
};
